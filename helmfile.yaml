# Main helmfile - references all service helmfiles
helmDefaults:
  atomic: false
  wait: true
  timeout: 10m

helmfiles:
  - path: secrets-chart/helmfile.yaml

  - path: postgres/helmfile.yaml.gotmpl

  - path: authentik/helmfile.yaml.gotmpl

  - path: vaultwarden/helmfile.yaml.gotmpl

  - path: homepage/helmfile.yaml

  # - path: bitwarden/helmfile.yaml
  # - path: immich/helmfile.yaml
